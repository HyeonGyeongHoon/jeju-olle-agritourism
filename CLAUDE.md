# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project

제주올레 도슨트 (Jeju Olle Docent) — a LangGraph-based RAG agent that turns a free-form natural-language query (visit timing/crop/region/theme) into a structured **B2B tourism-product proposal document** (기획서) for Jeju Olle trail courses: a spec/Market-Insight section, a timeline table with crop/harvest-season narration, a local-partnership ideas section, a climate-risk/Plan-B section, and a trust-tagging footer. Backed by Supabase (Postgres + pgvector), served over FastAPI SSE streaming. (The project pivoted from an earlier B2C chatbot design — see `docs/retrospectives/` for that history — so older code comments/docs may still describe the B2C framing; this file reflects the current B2B state.)

## Commands

```powershell
# Setup (Windows PowerShell)
python -m venv .venv
.venv\Scripts\Activate.ps1
pip install -r requirements.txt

# Run the API server
python -m uvicorn src.main:app --host 0.0.0.0 --port 8000
# -> POST /api/v1/report/generate {query: str} (SSE: event: node_progress / report / end / error),
#    GET /health, GET /docs (Swagger UI does NOT render SSE live — see Gotchas)

# Tests
python -m pytest                          # full suite
python -m pytest tests/test_router.py -v  # single file
python -m pytest tests/test_router.py::test_route_intent_classifies_opinion_about_specific_course_as_info_lookup -v  # single test

# Lint / format
ruff check .
ruff format .

# Re-ingest DB — all three are Gate B (irreversible: embedding API calls + DB writes), ask before running
python scripts/run_db_ingestion.py               # courses + course_chunks + sub_segments + wheelchair/safety
python scripts/run_culture_db_ingestion.py        # culture_crop_knowledge (crop/culture docs, title-keyed upsert)
python scripts/run_visitor_analytics_ingestion.py # visitor_analytics (PDF reports, year_month+region_dong upsert)
```

Required `.env` keys actually read by the code: `SUPABASE_URL`, `SUPABASE_KEY`, `UPSTAGE_API_KEY` (Solar chat + embeddings), `VISIT_JEJU_API_KEY`. There is no KMA/weather API key — `weather_client.py` is 100% local (static seasonal table + keyword simulator), not a real KMA call; don't add a `KMA_API_KEY` requirement back without actually wiring a real weather call. `.env.example` also lists `REPORT_API_KEY` (optional — see below), so it's these five keys, nothing else.

`REPORT_API_KEY` (optional): if set, `POST /api/v1/report/generate` (`src/main.py`) requires a matching `X-API-Key` header (checked with `secrets.compare_digest`); if unset, the endpoint accepts unauthenticated requests (fine for local dev, but leaves the endpoint open to unlimited paid Upstage API calls — set this in any environment reachable by untrusted clients). The endpoint also applies a simple in-memory sliding-window rate limit (5 requests/60s per API key, or per client IP when no key is presented) — single-process only, does not coordinate across multiple `uvicorn` workers/processes.

## Architecture

### LangGraph pipeline (`src/agent/`)

Entry point `agent_runtime` is built in `graph.py` and consumed by `src/main.py`'s SSE endpoint via `agent_runtime.stream(inputs)`. Fixed-path graph (11 nodes) with four intent-based conditional branches (a fourth, `route_after_retriever`, was added 2026-07-25 for the fail-fast exit described below):

```
intent_classifier → intent_parser → market_location_resolver ─┬─(non-recommendation)─────→ quick_responder ────────────────────────────────┐
                                                            └─(course_recommendation)→ safety_evaluator → retriever ─┬─(is_exit_early)──────┤
                                                                                                                       └─(normal)→ report_generator ┤
                                                                                                                                                     └→ quality_checker ─┬─(passed / loop_count≥3)─→ END
                                                                                                                                                                          └─(failed)─→ query_rewriter ─┬─(non-recommendation)→ tool_agent
                                                                                                                                                                                                            └─(course_recommendation)→ retriever (loop, max 3x)
tool_agent ⇄ tool_executor (function-calling loop, max depth 3)
```

`retriever`'s `is_exit_early` branch (`route_after_retriever` in `graph.py`) joins the same `quick_responder` node the non-recommendation intents use — see the `retriever`/`quick_responder` bullets below for what actually flows through that shared node in each case.

**Where the node functions live (changed 2026-07-26 — `src/agent/nodes.py` no longer exists):** the single 1860-line `nodes.py` was split into the **`src/agent/nodes/` package** (pure code movement, no behavior change — see the **Nodes package** section below for the split rationale, the local-import convention, and what it means for mocking). Which submodule holds which node:

| submodule | lines | contents |
|---|---|---|
| `nodes/intent.py` | 201 | `classify_intent_node`, `parse_intent_node`, `resolve_market_location_node` |
| `nodes/safety.py` | 67 | `evaluate_safety_node` |
| `nodes/retriever.py` | 485 | `retrieve_rag_node` + all search-policy helpers (`_filter_course_ids_by_target_course`/`_by_location`/`_by_crop`, `_course_row_matches_location`, `_crop_location_boost`, `_describe_target_course_mismatch`, `_build_fail_fast_result`) |
| `nodes/reporter.py` | 644 | `generate_report_node`, `quick_responder_node` + prompt-context helpers (`_build_course_meta_context_str`, `_build_culture_context_str`, `_build_market_insight_summary_str`, `_MARKET_METRIC_LABELS`, `_OUT_OF_SCOPE_DECLINE_MSG`) |
| `nodes/quality.py` | 235 | `check_quality_node`, `rewrite_query_node`, `_score_to_stars` |
| `nodes/tools.py` | 244 | `tool_agent_node`, `tool_executor_node` |
| `nodes/__init__.py` | 157 | re-exports the 11 node functions **plus every helper/client name that used to be a `nodes.py` module global** (`__all__` — 53 names total: the 11 node functions + 42 re-exported helpers/clients) — so `from src.agent.nodes import X` and `patch.object(nodes, "X")` keep working unchanged |

Note `nodes/tools.py` (node functions) is a **different file** from `src/agent/tools.py` (the LLM function-calling tool definitions) — both exist, don't confuse them.

**Where the helpers named in the bullets below actually live (changed 2026-07-26):** the DB-query/normalization helpers the nodes call — `_fetch_market_insight`, `_fetch_course_meta_by_name`, `_search_culture_knowledge`, `_execute_rdb_filtering`, `_resolve_stats_region_from_areas`, `_get_olle_relevant_admin_dongs`, `_looks_like_course_name`, `_resolve_canonical_region_dong`, `_query_visitor_analytics_row`, `_get_latest_available_year_month`, `_normalize_admin_tier_name`, `_LEGAL_DONG_TO_ADMIN_DONG` and friends — were extracted verbatim to `src/services/db_service.py` . A second, same-day extraction moved the price-estimation constants/helpers and `_resolve_effective_crops` to `src/services/price_estimation_service.py` (see the `report_generator` bullet). Bullets below that say "the shared X helper" or name one of these still describe the behavior correctly; only the file changed. See the **Service layer** section for the full inventory and the test-patching caveat it introduces.

- **`intent_classifier`** (`classify_intent_node` → `router.py`'s `route_intent`): classifies the query into 3 categories — `course_recommendation` / `info_lookup` / `other`. **(2026-07-26 consolidated from 5 categories down to 3)** the earlier `course_info` and `olle_general_info` categories were merged into `info_lookup`, with no legacy alias kept (`IntentCategory` in `src/models/schema.py` now has exactly these 3 members). This was a router-layer-only cleanup: every routing/node consumer in `graph.py`/`nodes.py`/`main.py` had already only ever branched on `== course_recommendation` or `== other` (or `!=`) — nothing in the codebase branched on `course_info`/`olle_general_info`/`info_lookup` individually, since all three collapsed onto the same `quick_responder` → `tool_agent` pipeline anyway — so downstream logic is unchanged; only `router.py`'s classification values and `SYSTEM_PROMPT` category descriptions moved. Where the categories' old names appear below (e.g. in dated bullets describing what a rule returned *at the time*), they're kept for continuity with the commit history/retrospectives rather than silently rewritten — see `docs/retrospectives/` for that history. Only attaches a label to `intent_category` — actual branching is done by `route_after_location_resolve` in `graph.py`. `course_recommendation` is the only intent that proceeds to the full pipeline (safety → retriever → `report_generator`); the other two (`info_lookup`/`other`) route to `quick_responder`.
  - `route_intent` is **two-stage** (2026-07-25): a rule-based precheck (`_rule_based_precheck`, **no LLM call**) runs first and settles three classes of query deterministically; only what it doesn't match falls through to the Solar LLM classification. (a) A **plain course-recommendation ask** — contains "코스"/"올레" + "추천", no 기획서/기획안/기획해/상품 기획/상품화 keyword, and no specific course named — is forced to `other`, because this service does not recommend individual courses and LLM classification was observed flip-flopping between `other` and `course_recommendation` across runs of the identical query. (b) Profanity, and 1–2-word greetings, are forced to `other`. (c) A query that **already names a specific course** *and* says "추천" (with no 기획서 keyword) is forced to **`info_lookup` (당시 이름 `course_info`, merged 2026-07-26), not a refusal** — "7코스 초보자한테 추천할 만해?" is an opinion/suitability question about that one course, not a request to pick a course — and `target_course` is filled from the regex at the same time. Don't "fix" (c) back into a refusal: over-refusing exactly this shape is the bug that motivated the whole precheck.
  - Course naming is detected by `_SPECIFIC_COURSE_PATTERN` (digits + optional `-digit`/`-letter` + optional "번" + "코스", so "1번 코스"/"10-1 코스" spellings match too) and normalized by `_normalize_course_name` to the DB `course_name` form (`1코스`/`10-1코스`/`3-A코스`) — that normalization is load-bearing because downstream `_filter_course_ids_by_target_course` compares with `==`. A course name that appears **only behind an exclusion marker** (말고/빼고/외에/이외/제외/아닌/아니고/대신 within 8 chars after the match, e.g. "1코스 말고 다른 코스 추천해줘") does not count as naming a course, so that query is still refused as (a).
  - The router prompt's `info_lookup` category (`src/agent/prompts/router.md:11`, 3rd of 3; absorbed the former `course_info`/`olle_general_info` split as sub-cases 3-a/3-b/3-c on 2026-07-26) covers **both** metadata lookups (3-a: distance/difficulty/time/start-end/stamps) **and** opinion/evaluation/suitability questions about an already-named course (3-b), plus general olle-trail info/culture/crop/visitor-stats lookups (3-c) — the metadata+opinion half is what makes the LLM classify "1코스 어때?" (no "추천" token, so the rule in (c) doesn't fire) as `info_lookup` instead of `course_recommendation`. When the LLM returns `target_course` as null (observed live, reproducible 3/3), `route_intent` backfills it from the same regex; an LLM-supplied value wins.
  - **(d) added 2026-07-26)** A query with `has_course_ref` (코스/올레) + `has_proposal_ask` (기획서/기획안/기획해/상품 기획/상품화) but **no** "추천" token and no specific course named — e.g. "당근 코스 기획서 써줘", "올레 코스로 상품 기획해줘" — is now forced deterministically to `course_recommendation`, no LLM call. Found live while verifying the `strict_single_crop` feature below: this exact query shape used to fall through to the 2-stage LLM classification (nothing in (a)/(b)/(c) matched it) and that LLM call was observed flip-flopping `course_recommendation`/`other` across identical repeated calls (3 runs: 2× wrongly `other`) — the same class of nondeterminism (a)'s "코스+추천" rule was already built to eliminate, just on a different keyword combination ("기획서" without "추천" instead of "추천" without "기획서"). Deliberately scoped narrow: a query where `has_recommend_ask` is *also* true ("당근 코스 추천해서 기획서 만들어줘") or where a specific course is named ("1코스로 기획서 만들어줘") still falls through to the LLM path unchanged — neither combination had an observed nondeterminism report, so their existing behavior (which tests already assumed) was left alone rather than folded into the new rule speculatively. See `_RULE_BASED_REASON["proposal_without_recommend_word"]` and the guard at `router.py:133`.
- **`intent_parser`**: separate LLM call that extracts RAG search filters into `B2BQueryParams` (`hard_constraints.wheelchair_required`, `vector_query`, `target_month`/`season`, `key_item_or_crop`, `preferred_location`, `concept_theme`, `target_audience`, `include_market_insights`, `strict_single_crop`). There is no `soft_constraints` (time/distance/difficulty) any more — that B2C-era relaxation mechanism was removed; the RDB filter is now hard-constraints-only, single pass, no fallback tiers. When the query names a region by a **visitor-statistic condition** instead of a place name (e.g. "외국인 관광객이 많았던 지역"), `preferred_location` is left null and `market_location_query` (metric/year/month/direction) is filled instead. Not the same thing as `intent_classifier` — don’t confuse the two. Note that its prompt tells it to fill `preferred_location` when a "지역/코스명" is named directly, so it does in practice put a **course name** into that region field ("1코스 괜찮을지 추천해줄래?" → `preferred_location='1코스'`, confirmed live 2026-07-25); both consumers (`quick_responder`, `retriever`) therefore null it out via `_looks_like_course_name` before using it as a region — see those bullets.
  - **`strict_single_crop` (bool, added 2026-07-26)**: true only when the query uses an explicit exclusive-single-crop phrasing — "당근**만**", "**오직** 마늘만을 활용한", "감귤**만**을 활용한" — via `src/agent/prompts/parse_intent.md` rule 11; a plain crop mention with no exclusivity marker ("당근 코스 기획서 써줘") stays `false`, which is a deliberately-confirmed behavior: an unmarked crop mention must still expose the course's full crop set (this is not a bug to "fix" toward stricter filtering). Defaults to `False` on LLM-parse failure — this field is a scope preference, not a safety hard-constraint, so unlike `wheelchair_required` it needs no separate keyword re-check on the fallback path. Consumed only by `report_generator` (see below); `retriever`'s RDB filtering does not read it.
- **`market_location_resolver`**: only runs when `market_location_query.metric` is set. Resolves the natural-language statistic condition to an actual region by querying `visitor_analytics` (via the Supabase query builder — `metric` is restricted to the `MarketLocationMetric` enum, so no SQL-injection path exists), restricted to administrative dongs that Olle courses actually pass through (`_get_olle_relevant_admin_dongs`, using `data/jeju_districts.csv` + a hardcoded 법정동→행정동 map for the urban "동" areas the CSV doesn’t disambiguate — see `_LEGAL_DONG_TO_ADMIN_DONG`; both it and `_get_olle_relevant_admin_dongs` moved to `src/services/db_service.py` on 2026-07-26). Writes the resolved region into `b2b_params.preferred_location` (and `market_location_resolution` for citing the reason later) so every downstream node treats it exactly like a directly-named region. Runs for both branches (`quick_responder` and the full pipeline) before the fork, since `quick_responder` also needs `preferred_location`/`market_location_query` resolved.
- **`quick_responder`** (`quick_responder_node`): handles all intents except `course_recommendation`, **plus** the new `is_exit_early` fail-fast rejection from `retriever` (see above) — answers requests for Jeju culture/crop knowledge or visitor-statistics *information itself* (not a report) with a short prose answer instead of the 5-section 기획서 format. Searches `culture_crop_knowledge` via the shared `_search_culture_knowledge` helper (also used by `retriever`) and `visitor_analytics` via the shared `_fetch_market_insight`, but does **not** touch `retrieved_chunks` (no course search) — that empty state is itself the signal `check_quality_node` uses to pick its chunk-less verification branch. Feeds into `tool_agent`.
  - **`is_exit_early` short-circuits first** (2026-07-25, checked before even `get_supabase_client()`, so ahead of the `other` check below): if `retriever` set the fail-fast flag, this node returns `exit_reason` verbatim (wrapped as `"요청하신 조건으로는 기획서를 작성할 수 없습니다. {reason}"`, matching `generate_report_node`'s older `if not chunks:` phrasing) as both `docent_answer` and `final_response`, with **no DB search at all**. This is a different case from the `other` intent below — here `intent_category` is still `course_recommendation`, it just didn't reach `report_generator`.
  - `intent_category == other` short-circuits at the top of the node: it returns `_OUT_OF_SCOPE_DECLINE_MSG` as both `docent_answer` and `final_response` **without searching anything**. Two very different queries land in `other` — totally unrelated ones ("제주도 날씨 어때?", "서울 맛집 추천해줘") and on-topic-but-refused course-recommendation asks ("당근 코스 추천해줘") — and neither should get a culture/statistics answer substituted for the refusal. The message text itself (2026-07-25, revised) is deliberately phrased as a generic "this question is out of scope, here's what the service actually does" statement rather than one anchored to "course recommendation" specifically — an earlier wording that opened with "이 서비스는 개별 코스를 추천해 드리지 않고..." read oddly for a totally unrelated query like a weather question, since it answered a refusal the user never asked for.
  - When `target_course` is set, `_fetch_course_meta_by_name` pulls that course's real DB row (`crops`, `administrative_areas`, plus the measured `total_distance_km`/`estimated_time_hours`/`estimated_time_text`/`difficulty`/`start_point`/`end_point`) into the new `course_meta` state field, and `_build_course_meta_context_str` injects those figures into the prompt as the *only* permitted basis for difficulty/suitability claims. Without the measured columns the LLM asserted things like "7코스는 비교적 평탄해 초보자도 좋다" out of thin air (the real 7코스 is 17.6km / 5–6h / difficulty 중) — verified live 2026-07-25. Fields with no value are omitted from the prompt rather than sent empty, and the prompt forbids adding terrain/stamina judgments that aren't in the data or recommending a different course/region.
  - Region-field pollution guard: `preferred_location` is nulled when `_looks_like_course_name` says the whole value is a course name (reuses `router.py`'s `_SPECIFIC_COURSE_PATTERN`), *then* backfilled from `course_meta.administrative_areas` via `_resolve_stats_region_from_areas`, which converts 법정리/법정동 → 행정동 through `_LEGAL_DONG_TO_ADMIN_DONG` and only accepts a candidate that `_get_olle_relevant_admin_dongs` confirms — **fail-closed to `None`** if nothing converts, since passing an unverified region is worse than passing none. The corrected `key_item_or_crop`/`preferred_location` are written **back into `state["b2b_params"]`**, not just held in locals: `tool_agent_node` reads `b2b_params` itself to decide tool calls, so a local-only fix left it calling the statistics tool with `region_dong='1코스'`, whose "조회 가능한 지역: ..." error list the LLM then surfaced as "대신 이 지역들을 추천드릴 수 있어요" — reintroducing the very other-course/region recommendation the routing rules exist to prevent.
  - `_fetch_market_insight`'s `target_month=None` handling changed 2026-07-25: it used to return `None` outright whenever the query didn't name a month, and this node fed it `target_month` after that field had already been defaulted to *today's* month — so "OO동 최근 방문객 수는?" (no month named) silently failed whenever `visitor_analytics` wasn't yet ingested through the current calendar month (confirmed live: DB ingested only through 2026-05, query ran on 2026-07-25), producing a wrong "통계를 찾지 못했습니다" answer plus 5–6 wasted `quality_checker` retry cycles. Fixed on both ends: `_fetch_market_insight` now returns that region's single most-recent row, unfiltered by month, when `target_month` is `None` (rather than failing), and this node now passes it the query's *actual* stated month (`b2b_params.get("target_month")`, `None` if unstated) instead of the already-defaulted-to-today value — the separate `target_month` used for crop-seasonality narration elsewhere in this node is untouched, since that's a different consumer of the same field name and defaulting it to today is correct there.
  - `_fetch_market_insight`'s region-name matching was also fixed 2026-07-25 (found live while re-verifying the `eup_myeon_dong_areas` fix above): it did `.eq("region_dong", region_dong)` — exact match only — while `_filter_course_ids_by_location` had just been made tolerant of suffix-omitted region names ("한림" matching "한림읍"), so a query like "한림 기획서 만들어줘" matched the right courses but Market Insight silently came up empty even though the 한림읍 row actually exists in `visitor_analytics`. Fixed by adding `_resolve_canonical_region_dong`, which only runs as a fallback when the exact-match query returns nothing: it fetches the distinct `region_dong` values and finds one whose suffix-normalized form (via the same `_normalize_admin_tier_name` helper `_filter_course_ids_by_location` uses) matches the input, then re-queries with that canonical name. Fails closed to `None` if the normalized match is ambiguous (not currently possible — all 43 `region_dong` values are suffix-unique) rather than guessing, since Market Insight numbers are quoted verbatim in the report. The normal (exact-match) path costs no extra query.
- **`safety_evaluator`**: **no real weather API involved** — combines a static per-month seasonal-climate table (`get_seasonal_climate_note`) with an LLM-based judgment call, `assess_weather_risk_from_query` (`weather_client.py`, reuses the Solar chat API — no new external integration). Only its **DANGER** verdict is allowed to override the seasonal reading. Until 2026-07-24 this was a naive substring match (`"태풍" in query`, etc.) that misfired on any query merely *mentioning* a weather-risk word — e.g. "작년 태풍 피해가 컸나요?" (a question about the past) triggered the same forced reroute as an actual "is it safe to go now" question. It's now a Solar LLM call classifying whether the query is really about a current/near-term risk to *this* visit, vs. a historical/hypothetical/advice-seeking mention; on LLM failure it fails closed to **SAFE** (not DANGER — the whole point of this function is eliminating false-positive reroutes, so failing to DANGER would just reintroduce the same problem under a different trigger). (There is no `get_current_weather` function and no `KMA_API_KEY` — don’t assume a live KMA call is happening here; this is still not a real weather API, just a smarter query-classifier than plain substring matching.)
- **`retriever`**: single-pass RDB filter on hard constraints only (no B2C-era soft-constraint relaxation tiers — those are gone for good, see the domain-rules note below). `_execute_rdb_filtering` itself only applies `wheelchair_required`; `target_course`, `preferred_location`, and `key_item_or_crop` are each applied afterward via `_filter_course_ids_by_target_course` / `_filter_course_ids_by_location` / `_filter_course_ids_by_crop`. (2026-07-26: `_execute_rdb_filtering` now lives in `src/services/db_service.py` ; the three `_filter_course_ids_by_*` helpers stayed at the node level as search-policy code, and now sit in `src/agent/nodes/retriever.py` next to `retrieve_rag_node` — see the Service layer section below.)
  - **Fail-fast policy (2026-07-25, replaces the old fail-soft/relaxation design):** if any of these three filters finds zero overlapping courses, `retrieve_rag_node` no longer drops that condition and keeps searching unfiltered — it **immediately returns** `{"is_exit_early": True, "exit_reason": "<specific reason>", "retrieved_chunks": [], "culture_chunks": [], "sub_segments": []}` via the shared `_build_fail_fast_result` helper, skipping vector search/culture search/Market Insight entirely for that request. `fallback_applied`/`fallback_reason` are now always `False`/`None` on this path — **the note in "Domain logic rules" below describing them as a "still-live" relaxation mechanism is obsolete; that mechanism was deliberately removed.** `graph.py`'s `route_after_retriever` checks `is_exit_early` and routes straight to `quick_responder` (skipping `report_generator`) so the rejection reaches the user in ~5s instead of going through a full report-generation + relevance-verification cycle. `quick_responder_node` checks `is_exit_early` at the very top (before any DB calls) and returns `exit_reason` verbatim as the answer; `tool_agent_node` and `check_quality_node` both also check the flag early (before their normal tool-call/verification logic) so the rejection isn't overwritten or sent through a wasted `query_rewriter` retry loop. This was a deliberate policy decision (confirmed with the user 2026-07-25) — don't reintroduce the old "relax and keep searching" behavior without discussing it first.
  - `target_course` mismatch has two sub-cases with different `exit_reason` text: (a) the named course exists but fails a hard constraint (e.g. wheelchair) — `_describe_target_course_mismatch` supplies the specific reason ("'2코스'에는 휠체어로 이용 가능한 구간이 없습니다"); (b) the name doesn't match any real `course_name` at all (e.g. "가파도" vs the real name "10-1코스") — a generic "코스명과 직접 일치하는 코스를 찾지 못해…" message is used. Both are fail-fast now (previously (b) fell back to unfiltered search).
  - `_filter_course_ids_by_location` was rewritten 2026-07-25 to fix a real bug the fail-fast policy exposed: some 법정리 names exist in **more than one** 읍/면 (e.g. 세화리 in both 구좌읍 and 표선면), and the old substring-match-on-`administrative_areas` approach could match the wrong region's course — which, under fail-fast, meant a rejection that should have fired (e.g. "구좌읍 감귤" — no real 구좌읍 course grows 감귤) got silently bypassed by a wrong-region course sneaking through. Fixed via a new `courses.eup_myeon_dong_areas` column (see Data layer below) that stores each course's actual 읍/면/동 without the ambiguity, backfilled by `scripts/backfill_eup_myeon_dong_areas.py`. Matching is now layered: (1) `course_name` substring match (unchanged), (2) exact token match against `administrative_areas` (법정리-level, for when a user names a specific 법정리 directly — strengthened from substring to exact-token to avoid the same class of collision), (3) exact token match against `eup_myeon_dong_areas` (the common case — a plain 읍/면/동-level query), tolerant of suffix omission ("한림" ↔ "한림읍") via `_normalize_admin_tier_name`. Legacy rows with an empty `eup_myeon_dong_areas` fall back to the old substring expansion. Two of 29 courses' backfilled values couldn't be auto-disambiguated by the backfill's anchor-consistency algorithm (no other unambiguous 법정리 token in the same course to resolve against) and were deliberately left with their full candidate set rather than guessed: course 17 (용담동 → 용담1동/용담2동, actually correct — the course really passes through both) and course 14-1 (서광리 → 우도면/안덕면, which **was** wrong — corrected by hand to `한경면,안덕면` after confirming live that 14-1 isn't a Udo course). If a new course is added whose `administrative_areas` contains an ambiguous 법정리 name, re-run the backfill script and check its "미해소" report before assuming the new column is populated correctly.
  - After hard filtering, pgvector similarity search runs via `match_course_chunks`. `retriever` also independently queries `culture_crop_knowledge` (via the `_search_culture_knowledge` helper — RPC `match_culture_chunks`, falling back to local JSON keyword search only if that RPC fails/is empty) and `visitor_analytics` (Market Insight, keyed off `preferred_location`/`target_month` — which may have been auto-filled by `market_location_resolver`). It applies the same course-name-in-region-field guard as `quick_responder` (`_looks_like_course_name` → `preferred_location = None`), so the full proposal pipeline is covered too: "1코스로 기획서 만들어줘" used to reach `_filter_course_ids_by_location` with `'1코스'`, which both produced a nonsense footnote and let the substring-based `_crop_location_boost` match "1코스" against **"10-1코스"**'s `course_name` and promote an entirely different course — the same course-name-prefix collision that made `_filter_course_ids_by_target_course` switch to exact matching. `target_course` remains the only intended channel for "this specific course".
  - **NULL-safe `crops`/`administrative_areas` in the chunk assembly (fixed 2026-07-27) — use `or ""`, never `.get(key, "")`.** The `chunks_data` dict built after the pgvector search read `course_meta.get("crops", "")`, but a `.get()` default only applies when the **key is absent**; a `courses` row whose nullable `crops`/`administrative_areas` column is SQL NULL comes back with the key *present* and the value `None`, so `None` was carried all the way to `report_generator`, where `_resolve_effective_crops` calls `.split(",")` on it and raises `AttributeError`. That call site (`nodes/reporter.py`) is not wrapped in a try, so the exception escaped the graph and `src/main.py`'s SSE stream died with `event: error` — the whole request lost, not just the crop list. Both ends now use the `or` form (`course_meta.get("crops") or ""` at `src/agent/nodes/retriever.py:211-212`, `(chunk.get("crops") or "").split(",")` at `src/services/price_estimation_service.py:139`), the same principle as the `float(... or 0.0)` that already guards `total_distance_km`/`estimated_time_hours` in that same dict. **Not reachable against the live DB today** — it has zero NULL rows in those columns (sparse courses store `""`), which is exactly why it had never fired; it was found by live QA while checking the new type declarations against the `courses` DDL during the 2026-07-27 state-typing pass, and fixed with user approval rather than left as a latent crash. Regression tests: `tests/test_retrieve_rag_node.py::test_retrieve_rag_node_treats_null_crops_and_areas_as_empty_string_instead_of_crashing` and `tests/test_price_estimation_service.py::test_resolve_effective_crops_returns_empty_list_when_crops_is_none`. Any new consumer of these two columns should assume `None` is possible — `RetrievedChunkDict`/`CourseMetaDict` in `state.py` now declare them `Optional[str]` for this reason.
  - **Known remaining gap**: the three fail-fast checks are each guarded by `if <condition> and course_ids:`, so if `_execute_rdb_filtering`'s hard-constraint filter (wheelchair) itself returns an empty list up front, none of the three fail-fast branches fire and the request falls through to `report_generator`'s older generic `if not chunks:` fallback message (no specific reason, and `check_quality_node`'s fail-fast short-circuit doesn't apply either since `is_exit_early` was never set — reopening the retry-loop risk this feature closes). Not reachable with the current DB (10 wheelchair-accessible courses exist), left as a known low-severity gap rather than fixed speculatively.
- **`report_generator`** (`generate_report_node`): writes the entire 5-section B2B report in one node. First sections 1–2 (product spec/Market-Insight, timeline table) via an LLM call — for `culture_crop_knowledge` rows that carry `target_crop`/`region_tag`/`active_months`/`season_stage` (currently only 7 of the ~26 crop/culture docs — see Data layer below), it compares `active_months` against the visit month and instructs the LLM to describe the crop as in-season or not-in-season accordingly, instead of always describing it as "currently in full bloom/harvest" (this in-season context string is built by the shared `_build_culture_context_str` helper, also used by `quick_responder`). Then, in the same call, sections 3–5 (partnership ideas, climate Plan A/B, Trust Tags): collects the **top-matched course's (`chunks[0]`) only** (crop × administrative_area) combinations (2026-07-26: narrowed from "every retrieved course" — see the "Local-partnership scope narrowed to top course" bullet below), dedupes them, and calls Visit Jeju's API for all unique combinations **concurrently** via `ThreadPoolExecutor` (2026-07-24: was sequential — one API call after another — so a report with many combinations accumulated real latency; results are cached per request either way to avoid duplicate calls across courses that share a combination). (2026-07-24: merged from two separate nodes, `docent_generator`(sections 1–2)/`report_finalizer`(sections 3–5, itself renamed from B2C-era `local_recommender`) — the conditional edge gating the second node on `intent_category == course_recommendation` was dead weight once `route_after_location_resolve` already guaranteed only `course_recommendation` ever reaches this part of the graph, so the two were combined and the router removed.)
  - **Crop cross-contamination fix (2026-07-26)**: when several courses were retrieved, the LLM sometimes attributed a *different* retrieved course's `crops` to the top-ranked course's product name/USP/timeline (observed live: top-matched 15-B코스, actual crop 마늘, got 14코스's 감귤/양배추 attributed to it) — this was the bug the 2026-07-25 retrospective deferred fixing. Fixed in the course-context assembly loop (**now `src/agent/nodes/reporter.py:~347`** after the 2026-07-26 nodes-package split; the pre-split chain of line numbers in the old single `nodes.py` was ~1287 → ~1393 → ~1856 reading backwards through that day's two service extractions): each course block now carries an explicit role label (`★ 최상위 매칭 코스` for `chunks[0]` — the only course this report actually productizes — vs `참고용 코스 (상품화 대상이 아님)` for the rest), and crops/administrative_areas are re-attributed sentence-by-sentence with the course name (`※ {course_name}의 재배작물: ..., {course_name}의 경유 행정구역: ...`) instead of just being placed near the course block. The report system prompt's 작성 원칙 also gained an absolute rule: only course 1 is productized, never borrow course 2+'s crops. (That prompt text is no longer a Python literal — since 2026-07-26 it lives in `src/agent/prompts/generate_report.md`; see the **Prompt resources** section.) Live-verified 2026-07-26 on 4 multi-course-match queries (two runs of "한림 기획서 만들어줘" plus "구좌읍"/"조천읍") against the actual `courses.crops` DB values — no cross-contamination in any of the 4.
  - **`strict_single_crop` exclusive-crop filtering (2026-07-26)**: a new helper `_resolve_effective_crops(chunk, b2b_params)` (**defined in `src/services/price_estimation_service.py:126`** since the 2026-07-26 price-helper extraction — called once from **`src/agent/nodes/reporter.py:448`** after that day's later nodes-package split; the call site was `nodes.py:1395` before the split, and the definition sat at `nodes.py:~1330`/`~1812` before the two service extractions) narrows the top-matched course's crop list to a single target crop when `b2b_params.strict_single_crop` is true **and** that target crop (`key_item_or_crop`) actually appears in `chunks[0]`'s real `crops` column — e.g. "당근만을 활용한 코스 기획서 써줘" against a course with `crops="감자,당근"` narrows to `["당근"]` only, so section 3's partnership ideas, the `unique_combos`/`_price_combo_set` counts feeding Visit Jeju lookups and Section 1's price estimate, and the main report LLM prompt all stop referencing 감자. All three consumers call the same helper once (cached as `_effective_top_course_crops`) rather than each re-deriving the narrowed list — three independent derivations previously caused the "priced combo count exceeds what section 3 shows" class of bug described in the bullet below, and this reuses that lesson. **Fail-soft, not fail-closed**: if the named crop isn't actually among the matched course's crops (e.g. "오직 한라봉만을 활용한 코스 기획서 써줘" against a course whose real crop is 녹차), the narrowing is skipped entirely and the full crop list is used as if `strict_single_crop` were false — collapsing to zero crops/combos over a user-named crop that just doesn't exist on this course was judged worse than ignoring the flag, consistent with how `_filter_course_ids_by_location`/`_filter_course_ids_by_crop` already treat a zero-overlap condition (that fail-fast policy is about *retrieval*-stage filtering finding no matching courses at all — this is a *description*-scope narrowing of an already-matched course, a different failure mode, so it fails soft rather than triggering `is_exit_early`). When the narrowing does apply, the main report LLM prompt also gets an added absolute rule (`strict_single_crop_rule_str`, built at **`reporter.py:~469`** and injected into the prompt at **`~493`** as one of `generate_report.md`'s 13 `.format()` placeholders; pre-split these were `nodes.py:~1416`/`~1487`, and `~1521`/`~1592`/`~2003` earlier that day) forbidding any mention of other crops in the product overview/timeline sections. Live-verified 2026-07-26 on 4 scenarios (exclusive-carrot, exclusive-garlic-with-other-matched-courses-present, non-exclusive-carrot regression, and the 한라봉/fail-soft case) — no cross-contamination, no regression on the unmarked-mention path, no broken/empty sections on the fail-soft path.
  - **Local-partnership scope narrowed to top course (2026-07-26, follow-up to the fix above)**: the `unique_combos` collection loop (**now `reporter.py:516`**; pre-split ~1509 in `nodes.py`, and ~1614/~2038 earlier that day) that feeds section 3's Visit Jeju lookups was changed from iterating every retrieved course to reading only `chunks[0]` — same rationale as the crop-attribution fix above (section 3's "로컬 상생 제휴 아이디어" should describe partnerships for the course this report actually productizes, not a blend across the whole retrieved set). **This reverses the note that used to be here** claiming section 3 mixing crops across all retrieved courses was intentional-by-design — it no longer is; don't revert this to iterate all `chunks` again without discussing it first. A related consistency bug was caught and fixed in the same pass: `_price_combo_set` (used to size Section 1's "예상 1인 단가" combo count, **now `reporter.py:449`**; pre-split ~1396, and ~1501/~1949 earlier that day) still looped over every `chunks` entry after `unique_combos` had already been narrowed to `chunks[0]` — meaning the priced combo count could exceed what section 3 actually showed. Fixed to also read only `chunks[0]`, keeping both counts in sync (regression test: `test_generate_report_node_prices_only_top_course_combos_not_other_chunks`).
  - Section 1's "예상 1인 단가 범위" (estimated per-person price range) is, as of 2026-07-25, **computed deterministically in Python** (`_estimate_price_range` — since the 2026-07-26 refactor this and all the other names in this bullet live in `src/services/price_estimation_service.py`, not `nodes.py`; the arithmetic is byte-identical, see the Service layer section), not left for the LLM to invent — the top-matched course's `estimated_time_hours`/`difficulty` and the count of unique (crop × administrative_area) local-partnership combos (capped at 3) are combined into a group-level cost, then divided by an assumed group size that varies by `b2b_params.target_audience` (`_PRICE_GROUP_SIZE_BY_AUDIENCE`: family=4/healing=2/active=6/senior=10/corporate=15) to get an actual **per-person** figure — dividing group-level docent-guide cost by headcount is the whole point, since a docent's fee doesn't scale with how many people are in the group, but the local-experience add-on does (it's already per-person). The result is injected into the prompt as a fixed fact the LLM must quote verbatim (same pattern as Market Insight numbers), not a "제안값" it can freely estimate — that distinction was intentionally moved from "제안값 예외" to "인용 대상" in the prompt's 작성 원칙. All rate constants (`_PRICE_BASE_FLAT`=10,000원, `_PRICE_BASE_RATE_PER_HOUR`=15,000원, `_PRICE_DIFFICULTY_MULTIPLIER`, `_PRICE_ADDON_PER_COMBO`=3,000원) are assumed placeholder values — there is no real docent-tour rate card anywhere in this codebase or its data sources, so these numbers reflect a deliberate business-rule guess (confirmed with the user 2026-07-25), not a fact derived from any dataset. `estimated_time_hours` (numeric, distinct from the display-only `estimated_time_text`) had to be added to the `chunks_data` dict built in `retrieve_rag_node`'s pgvector assembly step (it wasn't previously carried through to `report_generator`) — missing/zero hours fail soft to a 1-hour floor rather than raising.
- **`tool_agent`** (`tool_agent_node`) / **`tool_executor`** (`tool_executor_node`): function-calling agent loop entered after `quick_responder`. `tool_agent` decides which tools to call; `tool_executor` runs them (max depth 3). On quality-retry (`direct_retry`) `tool_agent` increments `loop_count`. Three things about this node are easy to break:
  - `intent_category == other` with no `tool_outputs` yet returns `{"tool_calls": None}` immediately, preserving `quick_responder`'s refusal. Otherwise this node would see leftover `key_item_or_crop`/`preferred_location` in `b2b_params`, auto-call the crop/statistics tools off those values alone, and regenerate an answer that is effectively the course recommendation the refusal just declined to give. Right alongside this (2026-07-25, checked before `pending_tool_calls` is computed — same ordering lesson as the `has_grounded_answer` fix below): `is_exit_early` with no `tool_outputs` yet also returns `{"tool_calls": None}`, so `quick_responder`'s fail-fast rejection isn't overwritten by a tool re-call made off the same leftover `b2b_params` values.
  - It reads `state["course_meta"]` (filled by `quick_responder`) into its own prompt, because **this node, not `quick_responder`, usually writes the answer the user actually sees** — any tool call at all makes it overwrite `final_response`. It also emits an explicit "don't offer a different course or region as an alternative; if a tool says a region/period is unavailable just say so briefly" instruction, since absolute-rule 2 ("kindly offer the available-options list back to the user") degraded into "here are other regions I can recommend instead" on course-specific queries (live QA 2026-07-25).
  - Both `system_prompt`s in this node (the depth≥3 fallback branch and the main tool-result-answer branch) were reworded 2026-07-25: the old framing ("친절한 도슨트 챗봇" for "탐방객과 기획자") let the LLM address the reader as a traveling tourist, so a plain visitor-statistics answer came back with lines like "혼잡도를 고려해 여유 있게 일정을 계획해 보세요! 😊" — out of place for this service's actual audience. This is the answer-writing node for the non-report intents (see above), so the audience framing lives here, not in `quick_responder`. Both prompts now state explicitly that the reader is a **지자체 담당자/여행사 기획자 (product planner)**, not a tourist, and forbid travel-advice/sentimental phrasing; the main branch also invites a short planner-angle remark instead (e.g. "성수기 관광 수요를 반영하므로 혼잡도 관리와 시간대별 분산 운영 전략이 필요합니다") — confirmed live 2026-07-25 on "외도동 최근 방문객 수는?".
  - If there are no tool calls to make **and** no tool outputs, this node has learned nothing new, so it must not regenerate: when `state` already has a grounded `final_response` (any of `course_meta`/`culture_chunks`/`market_insight` present) and this is not a quality retry, it returns `{"tool_calls": None}` and leaves the existing answer alone. It used to re-prompt the LLM with an empty context, overwriting `quick_responder`'s DB-grounded answer with generic world knowledge. The two paths deliberately left as-is: a `quick_responder` answer with *no* grounding at all ("찾지 못했습니다") still gets regenerated, and quality retries still fall through so `loop_count` keeps incrementing (infinite-loop guard).
    - This `has_grounded_answer` check must run **before** `pending_tool_calls` is computed, not after — until 2026-07-25 it sat after that computation and the early `return {"tool_calls": pending_tool_calls}` right above it, so it was effectively dead code: a grounded case is exactly the case where the `preferred_location`/`key_item_or_crop` used for that lookup is still sitting in `b2b_params`, so `pending_tool_calls` was reliably non-empty and returned before the grounded check was ever reached. Caught live (not planned work — found mid-session re-verifying the market-insight fix below): "구좌읍 3월 방문객 수는?" had `quick_responder` correctly ground the answer in the March 2026 row (460,038명), but this node then re-called the statistics tool anyway and overwrote it with the May row (564,611명), plus a stray "가상 데이터" disclaimer. Fixed by moving the `has_grounded_answer` check above the `pending_tool_calls` block. A second, compounding bug in the same code path: the re-call's `year_month` was computed from `market_location_query` only (the statistic-condition-inference field) and ignored `b2b_params.target_month` (the field a plain "region + month" query actually fills), so even a genuine re-call would silently drop the requested month and fall back to whatever month the tool defaults to — fixed by adding `market_query.get('month') or b2b_params.get('target_month')` as a fallback (assumes current year when only a month is given, same convention as `resolve_market_location_node`).
- **`quality_checker`** / **`query_rewriter`**: self-critique/rewrite loop, capped at `loop_count` 3. `check_quality_node` checks `is_exit_early` first (2026-07-25, ahead of every other branch including the "nothing retrieved" auto-pass below) and immediately returns `passed=True` with no LLM call — verifying a fail-fast rejection message against unrelated `culture_chunks`/`market_insight` content would be pointless and was exactly the kind of wasted retry loop this policy was meant to eliminate. Otherwise it branches on whether `retrieved_chunks` (course chunks) is present: if so it verifies against course facts *and*, since 2026-07-24, **relevance** — whether the recommended course actually matches the requested crop/location/month (`b2b_params`), not just whether it's internally consistent; if `retrieved_chunks` is empty but `culture_chunks`/`market_insight` are present (the `quick_responder` path), it verifies against that content instead of auto-passing; only when nothing was retrieved at all does it short-circuit to `passed=True`. **(Fixed 2026-07-26)** The chunk-present branch used to verify only course facts, leaving `market_insight`/`culture_chunks` out of its verification context entirely — unlike the chunk-less (`quick_responder`) branch, which already built that context via `_build_market_insight_summary_str`/`_build_culture_context_str`. That gap made the verifier flag a real, correctly-cited Market Insight number as "no source visible" (false-negative), triggering a wasted `query_rewriter` retry even when the report was factually fine. Fixed by calling those same two existing helpers (no new helper added) in the chunk-present branch too (**now `src/agent/nodes/quality.py:~107-110`**, reached across the package boundary from `reporter.py` via the local-import convention; pre-split ~1739 in `nodes.py`, and ~1841/~2258 earlier that day), so both branches now verify against the same market/culture context. Live-verified 2026-07-26: Market Insight numbers (한림읍 559,007명/구좌읍 564,611명/조천읍 721,347명) were quoted correctly in all 4 test queries and 3 of 4 passed quality_checker on the first try with no rewrite. **This did not make retries fully deterministic** — quality_checker is still an LLM-based re-verification, so pass/fail can still vary run to run (a pre-existing characteristic, not a new regression): "한림 기획서 만들어줘" measured 0 retries/29.4s on one run and 1 retry/81.8s on another, down from the pre-fix 2 retries/45s baseline, but not a guaranteed 0. `query_rewriter` only revises `vector_query` now — it used to also ask the LLM for `revised_soft_constraints` (max_time_hours/max_distance_km/difficulty) and store it, a leftover from the removed B2C soft-constraint mechanism that no downstream code ever read; that request/storage was deleted 2026-07-24. The edge *after* `query_rewriter` (`route_after_rewrite`) sends the retry back to whichever node the query originally entered through — `tool_agent` for the non-recommendation category, `retriever` otherwise — not always straight to `retriever`. A resolved `market_location_resolver` region persists across retries either way.
- State shape lives in `state.py` (`AgentState` TypedDict) — every node returns a partial dict merged into this state. `course_meta` (added 2026-07-25, also initialized in `src/main.py`'s initial state) carries the named course's measured DB row from `quick_responder` to `tool_agent`; it is the only cross-node channel for those figures, so a node that answers about a specific course without reading it will fall back to guessing. `is_exit_early`/`exit_reason` (also added 2026-07-25, also initialized to `False`/`None` in `src/main.py`) carry the retriever fail-fast signal described above from `retriever` through `quick_responder`/`tool_agent`/`check_quality_node`.
  - **Every nested dict on the state is now its own TypedDict (typing strengthened 2026-07-27 — `state.py` is the only file that changed, and there is no runtime behavior change).** The fields that used to be declared `Dict[str, Any]`/`List[Dict[str, Any]]` now name their actual keys: `HardConstraints`, `ParsedConstraints`, `MarketLocationQueryDict`, `MarketLocationResolutionDict`, `B2BQueryParamsDict`, `CourseMetaDict`, `WeatherInfoDict`, `SafetyCheckDict`, `RetrievedChunkDict`, `CultureChunkDict`, `SubSegmentDict`, `MarketInsightDict`, `RecommendationDict`, `QualityReportDict`, `ToolCallDict`, `ToolOutputDict` (16 nested, plus `AgentState` itself = 17 public TypedDicts). (`state.py`'s own module docstring dates this 2026-07-26 because the session ran past midnight — same change, not two.) **A TypedDict is a plain dict at runtime**, so `b2b_params.get("preferred_location")` / `chunk["crops"]` / `"x" in state` and every node's partial-dict return work exactly as before — node code (`src/agent/nodes/*.py`) and **test files were changed by zero lines** when this went in (290/290 still passing at that point). Each nested TypedDict's docstring records **who fills it and who reads it, file by file** — read that before adding or consuming a state key, it's faster than grepping.
  - **Required-vs-optional key convention (`total=True` base + `total=False` subclass) — this is deliberate, don't "modernize" it to `NotRequired`.** This repo targets Python 3.10 (`pyproject.toml`'s `target-version = "py310"`) and `typing.NotRequired` is 3.11+. `typing_extensions` *is* importable here as a transitive pydantic dependency, but it is **not** listed in `requirements.txt`, so rather than quietly take on a new declared dependency, `state.py` expresses the same thing with stdlib only: keys that always exist go in a `total=True` base class (`_XxxRequired`), and keys that may be absent are added in a `class XxxDict(_XxxRequired, total=False)` subclass. Three dicts need the split (`B2BQueryParamsDict`, `WeatherInfoDict`, `MarketInsightDict`); the rest are uniformly all-required or all-optional and need only one class. `__required_keys__`/`__optional_keys__` were verified live to come out as intended, as was `get_type_hints(AgentState)` returning exactly 24 fields matching the graph's 24 channels 1:1. Note that **key presence and value nullability are separate axes** — a required key can still hold `None`, which is written `Optional[...]`, not `total=False`.
  - Required-vs-optional was decided from **how consumers actually read the key**, not from the table DDL — e.g. `MarketInsightDict` marks `year_month`/`region_dong`/`total_visitors` required because consumers read them with brackets (`market_insight['region_dong']`, so a missing key is a `KeyError`), while the ratio metrics are `.get()` + `is not None` because they're NULL for most 행정동. Four declaration details worth knowing, all of them checked against the running code rather than assumed:
    - **`QualityReportDict`'s feedback key is `feedback`** — not `reason`, not `suggestions`. The verification prompt's response format and every consumer (`rewrite_query_node`, `tool_agent_node`) agree on `feedback`. All three of its keys (`passed`/`score`/`feedback`) are optional, because on the normal path the dict is `json.loads(llm_output)` verbatim with nothing guaranteeing any key survived — which is precisely why `should_continue` reads `report.get("passed", False)` and fails **closed** on a malformed report.
    - `WeatherInfoDict` carries three fields that were not in the original plan (`temperature`/`precipitation_mm`/`wind_speed_ms`) because all three `weather_client.py` construction sites really do fill them; they are fixed constants with **no consumer today**, and are declared required anyway since the key always exists. `guideline` is the one optional key — only `get_seasonal_climate_note`'s normal path sets it, while its `_safe_default` path and both `assess_weather_risk_from_query` returns omit it (hence `weather.get("guideline") or "<기본 문구>"` at the read site).
    - `market_location_resolution` is the only optional key on `B2BQueryParamsDict` because **it does not exist on the `B2BQueryParams` Pydantic model at all** — `resolve_market_location_node` injects it into the dumped dict at runtime, so on a query with no statistic-based location condition the key is simply absent. The other 9 keys are required because `model_dump` always emits them (values may be `None`).
    - `ParsedConstraints` is **intentionally declared narrower than the runtime dict**, and that is not a bug to "fix". `parse_intent_node` stores the whole LLM JSON there (`target_month`/`season`/`key_item_or_crop` and ~9 more), but **no code reads any of those from `parsed_constraints`** — they're all read from the Pydantic-validated `b2b_params` instead — so only the two keys that are actually consumed (`hard_constraints`, `vector_query`) are declared. A type checker won't complain about the extra keys either, since the value assigned is `json.loads(...)`, i.e. `Any`.

### SSE streaming (`src/main.py`)

`report_event_generator` (endpoint: `POST /api/v1/report/generate`) runs the graph in a background thread (`loop.run_in_executor`) and streams events **live** as each node actually completes, via an `asyncio.Queue` the background thread pushes into with `call_soon_threadsafe` — it does not wait for the whole graph to finish first. Per node completion it emits `event: node_progress` (`{node, label}`, label text from `NODE_PROGRESS_LABELS`); once some node has set `final_response` (usually `report_generator` on the course_recommendation path, or `tool_agent` on the other two intents' (`info_lookup`/`other`) path), the queue drains and it emits one `event: report` (the complete Markdown 기획서, not token-by-token) followed by `event: end`. On exception, `event: error` instead. There is no typing-effect/token-pacing simulation — that belonged to an earlier, now-deleted `/chat/stream` endpoint.

If the client disconnects mid-stream (closed tab, etc.), Starlette throws `GeneratorExit`/`CancelledError` into `report_event_generator` at its `await queue.get()` suspension point; a `finally: cancel_event.set()` there signals the background thread (via `_stream_until_cancelled`, checked before consuming each node's result from `agent_runtime.stream()`) to stop advancing to further nodes. This can't interrupt an already-in-flight `requests.post` inside the current node, but it does stop a query-rewriter retry loop from burning through its remaining paid API calls after nobody is listening anymore.

`node_output = event[node_name] or {}` — the `or {}` is load-bearing, not defensive-programming boilerplate. If a graph node returns an empty dict `{}` (no state keys changed — `market_location_resolver` does this whenever there's no statistic-based location condition, which is the common case), LangGraph's `stream()` surfaces that step's event value as `None`, not `{}`. Without the guard, `node_output.get("final_response")` throws `AttributeError: 'NoneType' object has no attribute 'get'` and the whole SSE response dies mid-stream (confirmed in production 2026-07-24, reachable from a plain course-recommendation query with no location/crop condition). Any new node you add that can legitimately return `{}` needs this same awareness downstream.

### External integrations, all with defensive fallback to mock/SAFE data on failure

- `src/agent/llm_client.py`: Upstage Solar chat completions, used by 7 different call sites (intent parsing, safety weather-risk assessment via `weather_client.py`, docent generation, quality check, query rewrite, `quick_responder` answer generation, `tool_agent` function-calling/answer generation).
- `src/agent/weather_client.py`: KMA 기상특보 조회서비스 (`WthrWrnInfoService/getWthrWrnList`, **not** the differently-named service some old code/docs may reference). Region filtering is by `stnId` (station code: 184=제주시, 189=서귀포시 via `_resolve_stn_id`) — the API's warning titles never contain place names, so text-matching on the title for a location is a dead end.
- `src/ingestion/visit_jeju_client.py`: Visit Jeju Open API for local cafe/restaurant recs, falls back to a hardcoded mock DB.
- Both HTTP clients call `truststore.inject_into_ssl()` at import time — required in this dev environment because the local network's TLS-inspecting proxy issues a cert that `certifi`'s bundle doesn't trust but the OS trust store does.

### Service layer (`src/services/`)

**Added 2026-07-26 as two separate pure refactors — no behavior change in either.** `src/agent/nodes.py` had accumulated DB access, price arithmetic, and node logic in one file; both extractions moved code **verbatim** (2447 → 1965 → 1860 lines) into `src/services/` (`src/services/__init__.py` is empty). (Later the same day the remaining 1860-line `nodes.py` was itself split into the `src/agent/nodes/` package — that third refactor is described in **Nodes package** below; where this section says "`nodes.py`" it means the pre-split file, and the node-side code it refers to now lives in one of the package submodules.) Both were live-verified after the move (see the change-history table below). Nothing about node routing, prompts, query semantics, or the price formula changed — if you're chasing a behavior difference, neither move is it.

**`db_service.py`** (538 lines, extracted first) — DB-query/normalization helpers. What lives in it now: `_looks_like_course_name`, `_execute_rdb_filtering`, `_fetch_market_insight`, `_fetch_course_meta_by_name`, `_search_culture_knowledge`, `_resolve_stats_region_from_areas`, `_get_olle_relevant_admin_dongs`, `_get_latest_available_year_month`, `_query_visitor_analytics_row`, `_resolve_canonical_region_dong`, `_load_legal_to_admin_mapping_from_csv`, plus the dependency helpers/constants they need (`_JEJU_DISTRICTS_CSV_PATH`, `_LEGAL_DONG_TO_ADMIN_DONG`, `_ADMIN_DONG_TO_LEGAL_DONGS`, `_ADMIN_TIER_SUFFIXES`, `_normalize_admin_tier_name`, `_COURSE_META_COLUMNS`, `_get_known_crop_tags`, `_crop_label_matches`, and the whole local culture-knowledge JSON fallback chain — `_LOCAL_*` path constants, `_TITLE_TRAILING_PARTICLES`, `_CITATION_MARKER_RE`, `_title_keywords`, `_load_local_culture_docs`, `_search_local_culture_docs`). The dependencies came along to avoid a circular import and to keep the data-access layer cohesive.

What deliberately **stayed** at the node level, because it's presentation or search-policy rather than data access: `_MARKET_METRIC_LABELS`, `_build_course_meta_context_str`/`_COURSE_META_DISPLAY_FIELDS` (all three now in `nodes/reporter.py`), and `_filter_course_ids_by_*`/`_course_row_matches_location`/`_crop_location_boost` (now in `nodes/retriever.py`). `get_supabase_client()` calls also stayed at the node level — every moved helper already took `client` as a parameter, so no call signature changed.

- **Mocking/patching caveat — `db_service`-internal calls (this broke 7 tests during the move; still true today — read before writing a test):** the nodes package re-exports the moved names (`from src.services.db_service import X` in `nodes/__init__.py`), so `patch.object(nodes, "_fetch_market_insight")` works **when a node function calls that name directly**. It does **not** intercept calls made *between* helpers inside `db_service` — e.g. `quick_responder_node` never calls `_get_olle_relevant_admin_dongs` itself, it reaches it indirectly through `_resolve_stats_region_from_areas` (a `db_service` internal), so that one must be patched at `src.services.db_service`. **This limitation is unchanged by the 2026-07-26 nodes-package split** — that split fixed a *different* patch-target problem (calls between one node submodule and another; see **Nodes package** below). Rule of thumb, still: patch where the *caller* resolves the name.
- `src/agent/tools.py` now imports from both modules — `_MARKET_METRIC_LABELS` from `src.agent.nodes`, and `_get_olle_relevant_admin_dongs`/`_get_latest_available_year_month`/`_search_culture_knowledge` from `src.services.db_service`. `scripts/backfill_eup_myeon_dong_areas.py` imports `_LEGAL_DONG_TO_ADMIN_DONG` from `db_service` too.
- New test file `tests/test_db_service_layer.py` covers the extracted layer directly (+3 tests over the 265 baseline → 268).

**`price_estimation_service.py`** (144 lines, extracted second, same day) — the Section-1 per-person-price arithmetic, pulled out of `generate_report_node`'s body. Holds 7 constants (`_PRICE_BASE_FLAT`, `_PRICE_BASE_RATE_PER_HOUR`, `_PRICE_DIFFICULTY_MULTIPLIER`, `_PRICE_ADDON_PER_COMBO`, `_PRICE_ADDON_MAX_COMBOS`, `_PRICE_GROUP_SIZE_BY_AUDIENCE`, `_PRICE_AUDIENCE_LABEL`) and 4 functions (`_compute_price_breakdown`, `_estimate_price_range`, `_build_price_breakdown_str`, `_resolve_effective_crops`). The moved code is byte-identical to `git show HEAD:src/agent/nodes.py` — not one character of the arithmetic changed.

- **Why `_resolve_effective_crops` lives here despite the name**: it reads like crop logic, not price logic, but it's the front half of the same calculation — it decides the (crop × administrative_area) **combo count** that `_estimate_price_range` is then priced off. It has exactly one call site in the whole codebase, in `generate_report_node`, which caches it once as `_effective_top_course_crops` (`src/agent/nodes/reporter.py:448`) and reuses that one result in three places: the `_price_combo_set` combo count, section 3's Visit Jeju lookups + partnership-idea prompt, and the `strict_single_crop_applied` absolute-rule decision. Keeping it next to the pricing code is what makes that "all three consumers share one judgment" constraint readable in a single file — re-deriving it independently is the bug class described in the `report_generator` bullets.
- These are **pure functions**: they know nothing about `AgentState`, Supabase, or any external API, and read only the plain dicts passed in (`course`/`chunk` = a `courses` row, `b2b_params` = a `B2BQueryParams` dump). That's why the new tests need no mocking at all.
- The nodes package imports only 3 of the 4 functions (`_estimate_price_range`, `_build_price_breakdown_str`, `_resolve_effective_crops` — re-exported via `nodes/__init__.py`, consumed in `nodes/reporter.py`) and none of the constants — `_compute_price_breakdown` is only ever called by the other two *inside* the service module, and the `_PRICE_*` values are never read at the node level.
- **No patch-target trap this time**, in deliberate contrast to `db_service.py`'s caveat above: no existing test mocked any of these 4 functions or 7 constants (all of them were verified by direct call), so nothing broke on the move. The same resolution rule still applies if a future test does need to mock one: `patch.object(nodes, "_estimate_price_range")` works for the node's direct calls, but intercepting the intra-module `_estimate_price_range`/`_build_price_breakdown_str` → `_compute_price_breakdown` hop requires patching `src.services.price_estimation_service`.
- **(2026-07-27)** `_resolve_effective_crops` gained one `or ""` NULL guard (`price_estimation_service.py:139`, see the `retriever` bullet above for the crash path it closes) — that is the only edit to this module since the extraction, so the *arithmetic* is still byte-identical to `git show HEAD:src/agent/nodes.py`; the "byte-identical" claim above now means the price formula, not literally the whole file.
- New test file `tests/test_price_estimation_service.py` (22 tests, 268 → 290; 23 tests / 292 total after the 2026-07-27 NULL-crops regression test) calls the pure functions directly — group-size-by-audience, difficulty multiplier, the missing-hours fail-soft floor, the add-on combo cap, and `strict_single_crop`'s narrowing + fail-soft. `tests/test_generate_report_node.py` changed by exactly one line (its import path); `generate_report_node`'s call sites were not touched at all.

### Nodes package (`src/agent/nodes/`)

**Added 2026-07-26 as a third pure refactor that same day — no behavior change.** The 1860-line `src/agent/nodes.py` was split into a 6-submodule package; **the file `src/agent/nodes.py` no longer exists**. See the submodule table in the LangGraph-pipeline section above for what went where. Nothing about routing, prompts, query semantics, or the price formula changed.

- **Import compatibility is a contract, not a convenience.** Before the split, the node functions *and* every helper/client they use (`get_chat_completion`, `get_supabase_client`, `_fetch_market_insight`, …) were all globals of one module, so callers wrote `from src.agent.nodes import X` and tests wrote `patch.object(nodes, "X")`. `nodes/__init__.py` re-exports all 57 of those names (`__all__`) to keep that namespace byte-identical. Don't remove a name from `__all__` because it "looks unused" — it's likely a mock target or a cross-submodule reference.
- **Local-import convention (the load-bearing design decision).** When a submodule needs one of those shared names, it imports it **inside the function body** (`from src.agent.nodes import X`, i.e. through the package `__init__`), *not* at module top level. Each submodule carries a "로컬 임포트 규약" comment saying so. Because the name is resolved at **call time** against the package attribute, `patch.object(nodes, "X")` swaps the object the running node actually uses. A top-level `from ... import X` in each submodule would have bound the original at import time and silently defeated every existing mock. There are 12 such call sites (intent 3, quality 2, reporter 2, retriever 3, safety 1, tools 1).
- **Consequence — `patch.object(nodes, X)` works again without exception.** All 290 tests passed with **zero test-file edits**, in deliberate contrast to the `db_service.py` extraction earlier the same day, which broke 7 tests. The two cases are genuinely different and both rules are live:
  - *Between node submodules* (e.g. `quality.py` calling `reporter.py`'s `_build_culture_context_str`, or any node calling `get_chat_completion`): the local-import convention routes the lookup through `src.agent.nodes`, so **patching `nodes` is correct and sufficient**.
  - *Between helpers inside `db_service`* (e.g. `_resolve_stats_region_from_areas` → `_get_olle_relevant_admin_dongs`): still **not** interceptable via `nodes`; patch `src.services.db_service`. The nodes split did nothing to change this.
  - The verification for this was a mock-injection script proving 12/12 local-import sites are actually intercepted — not merely "the suite is green", since a green suite can also mean the mock was never reached.

### Prompt resources (`src/agent/prompts/`)

**Added 2026-07-26 alongside the nodes split.** Seven large system prompts that were triple-quoted literals inside node bodies and `router.py` are now `.md` files loaded by `load_prompt(filename)` (`src/agent/prompts/loader.py`). Editing prompt wording no longer means touching Python.

| file | loaded by |
|---|---|
| `router.md` | `router.py:201` (`route_intent`'s LLM stage — replaces the deleted hardcoded `SYSTEM_PROMPT`) |
| `parse_intent.md` | `nodes/intent.py:70` |
| `generate_report.md` | `nodes/reporter.py:480` |
| `local_ideas.md` | `nodes/reporter.py:559` |
| `quick_responder.md` | `nodes/reporter.py:257` |
| `tool_agent.md` | `nodes/tools.py:232` |
| `tool_agent_fallback.md` | `nodes/tools.py:159` (the depth≥3 branch) |

- **The extraction is partial — `nodes/quality.py` still holds three inline triple-quoted prompts** (`quality.py:114` chunk-present verification, `:143` chunk-less/`quick_responder` verification, `:199` `rewrite_query_node`); that file imports no `load_prompt` at all. Don't assume "all prompts are in `prompts/`" when hunting for wording to change.
- **`generate_report.md` is the only templated one**: it keeps `{placeholder}` markers and the call site applies `.format(...)` with **13** keyword args (`weather_description`, `weather_warnings`, `culture_context_str`, `segments_str`, `market_insight_context_str`, `target_audience`, `emphasis_instruction`, `location_resolution_str`, `price_range_str`, `price_breakdown_str`, `fallback`, `reason`, `strict_single_crop_rule_str`). Any literal brace added to that file must be escaped `{{`/`}}` or `.format()` will raise. The other six are loaded verbatim, no formatting.
- `load_prompt` resolves paths relative to its own file, so uvicorn can be started from any working directory, and it **raises `FileNotFoundError` rather than returning `""`** — a silently-empty system prompt would ship a structurally broken report to the user (fail-closed).
- File convention: the `.md` files end with a trailing newline (POSIX), and `load_prompt` strips exactly one so the loaded string matches the old literals, which ended right after the last sentence. A prompt that genuinely must end in a newline needs a second blank line in the file.
- Six of the seven were verified byte-identical to their `HEAD` literals. `generate_report.md` differs by 2 intended lines only, from the f-string → `.format()` conversion (two defaults moved out of the prompt text into call-site argument defaults; no semantic change).
- **`router.md` looks very different from `git show HEAD:src/agent/router.py`, and that is not this refactor's doing** — `HEAD` predates the (uncommitted) 5→3 intent-category consolidation documented above. `router.md` matches the current 3-category design.

### Data layer (`src/ingestion/database_loader.py`, `supabase/schema.sql`)

Supabase Postgres + pgvector. `courses` table carries `crops`/`administrative_areas` (comma-separated strings, not normalized) used both for RAG context and for driving `report_generator`'s per-combination Visit Jeju API lookups. `scripts/run_db_ingestion.py` re-chunks course text and re-embeds via Solar (4096-dim) — irreversible/costly, see Gate B below.

Two more tables were added later and are now populated: `culture_crop_knowledge` (crop/culture narration docs — see `schema.sql` sections 8/8-1; `crop_name` is legacy, `target_crop`/`region_tag`/`active_months`/`season_stage` are the newer columns, only populated for the 7 docs sourced from `data/culture_knowledge/crop_seven_docs.json`) and `visitor_analytics` (Jeju Tourism Organization monthly visitor-pattern PDF data, section 9 — `region_dong` is 행정동/읍/면 level, a different administrative tier from `courses.administrative_areas`' 법정리/법정동 level; see `_LEGAL_DONG_TO_ADMIN_DONG` in `src/services/db_service.py` for the mapping between the two — it lived in `nodes.py` until the 2026-07-26 service-layer extraction). Both tables' DDL sat unexecuted in `schema.sql` for a while before actually being run in the Supabase SQL editor — if a fresh Supabase project ever throws `relation ... does not exist` for either, that's why; there's no automated way to apply `schema.sql`, it must be run manually in the SQL editor.

`courses.eup_myeon_dong_areas` (section 10, added and backfilled 2026-07-25) is a third administrative tier alongside `administrative_areas` (법정리/법정동) and `visitor_analytics.region_dong` (행정동/읍/면) — it exists specifically to resolve 법정리 names that are ambiguous across multiple 읍/면 (see the `retriever`/`_filter_course_ids_by_location` bullet above for why). It's populated by `scripts/backfill_eup_myeon_dong_areas.py --apply` (dry-run by default; re-run and check the "미해소" report if a new course is added), not by the main ingestion scripts — don't expect `run_db_ingestion.py` to touch it.

## Known environment gotchas

- Windows dev machine: `python`/`python3` may open the Microsoft Store instead of running — disable the App Execution Alias for `python.exe`/`python3.exe` if that happens.
- Supabase table/RPC DDL in `supabase/schema.sql` is never auto-applied — there is no raw-SQL execution channel via the REST client, so each new section (e.g. the 8-1 `culture_crop_knowledge` ALTER, or the `visitor_analytics` DDL) must be run by hand in the Supabase SQL editor before the corresponding ingestion script will work. Don't assume a table exists just because its `CREATE TABLE` has been sitting in this file for a while.
- `ruff check .` is **not** clean on this repo: it reports a nonzero and slowly growing number of `E501` (line-too-long) findings, because `pyproject.toml` sets `line-length = 88` with `select = ["E", "F", "I"]` and `ignore = []`, while the house style writes long explanatory Korean comments. So Gate C's `ruff check .` will not come back empty on this repo — recent sessions have compared against the previous E501 count and treated "no new error *categories*" as the bar, but that reading has never been confirmed with the user, so ask rather than assume when a change adds E501s. (Current reference point for that comparison: **1340 findings as of 2026-07-27**, across exactly six categories — E501 / I001 / F401 / E402 / F541 / F841. When asked on 2026-07-27 about +25 new E501s that were all Korean docstring lines in `state.py`, the user chose to accept them uncompressed. Note the count is inflated by ruff measuring East Asian characters as width 2, so many flagged Korean lines are under 88 actual characters.) Whichever way it goes, the fix for the noise is a deliberate decision about `line-length`/`ignore`, not reflowing every Korean comment.
- **Prompt-instruction leakage into user-facing output** — a recurring cosmetic class, no wrong facts, none of it yet addressed. Known instances:
  - `tool_agent_node`'s internal `[주의]` instruction (don't recommend other courses/regions) occasionally comes out verbatim in the answer.
  - (observed 2026-07-26) `report_generator` sometimes emits internal directive/debug strings between sections, e.g. `조건 완화 적용 여부: False` and `(출력 종료)`. The first traces to `src/agent/prompts/generate_report.md:53`, which injects the `{fallback}`/`{reason}` placeholders and tells the LLM to footnote the reason *only when True* — but **since the 2026-07-25 fail-fast policy `fallback_applied` is always `False` on this path**, so that line can now only ever leak, never usefully fire. Worth considering for deletion, but that's a prompt change, not a doc fix — left alone deliberately.
  - (observed 2026-07-26) Plan B sentences occasionally splice awkwardly ("...권장하세요.으로 전환") — the LLM concatenating an instruction fragment with generated text.
  - All three predate and are unrelated to the nodes-package/prompt-resource refactor; moving the prompts into `.md` files changed none of this text. Likewise, the `quality_checker` retry loop costs 1–2 extra Solar calls per request on the `quick_responder` path fairly often; that's expected, not a malfunction.
- The real Visit Jeju API (`src/ingestion/visit_jeju_client.py`) is currently unreachable in this dev environment (network firewall and/or slow upstream response, confirmed 2026-07-24) — it always falls through to the mock DB anyway, so this is harmless functionally, but `get_visit_jeju_recommendations` deliberately keeps `timeout=4` and `max_retries=2` (not the originally-longer 10s/3-retry combo) so a report with several crop×area combinations doesn't accumulate tens of seconds of retry latency before falling back. Don't casually bump these back up without checking whether the firewall/API issue is still present.
- **(Resolved 2026-07-26, deferred 2026-07-25)** Two bugs the 2026-07-25 session found live but deliberately deferred (documentation-only that day, per user decision — see memory `deferred-bugs-2026-07-25` and the retrospective's "남은 작업" section for the original deferred write-up) were fixed and live-verified the next day: (1) `generate_report_node` mixing a *different* retrieved course's `crops` into the top-ranked course's product name/USP/timeline, and (2) `check_quality_node`'s chunk-verification branch not including `market_insight`/`culture_chunks` in its verification context (causing false-negative rewrite loops on correctly-cited numbers). See the `report_generator`/`quality_checker` bullets above ("Crop cross-contamination fix (2026-07-26)" / "Fixed 2026-07-26") for the current-state fix detail, and `docs/retrospectives/2026-07-25_retrospective.md` section 4 (2026-07-26 follow-up) for the full live-QA verification writeup. Retry nondeterminism on the quality_checker loop remains — this fix reduced it, it did not eliminate it.

## Harness / safety gates for agents working in this repo

(Adapted from `.agents/AGENTS.md`, the project's agent-rules file for its own AI pair-programming tool — kept in sync here for Claude Code.)

**Gates (Safety Gates)**
- **Gate A (Plan before acting)**: before modifying core logic, assess the blast radius and lay out a plan before starting.
- **Gate B (Irreversible-action control)**: never auto-run, without explicit prior user approval, any of: DB ingestion (`scripts/run_db_ingestion.py`, `scripts/run_culture_db_ingestion.py`, `scripts/run_visitor_analytics_ingestion.py`), embedding API calls, `git push`, external network requests, or bulk file deletion. Also covers running the ALTER/CREATE statements in `supabase/schema.sql` against the live Supabase project (must be done manually in the SQL editor regardless — see gotchas above — but still confirm with the user before telling them to run a specific block).
- **Gate C (Quality gate)**: after a code change, run `pytest` and a syntax/lint check (`ruff check .`) to confirm it actually works before calling it done.
- **Gate D (Secrets protection)**: never let API keys, DB passwords, or other sensitive/personal info leak into source files or the conversation.
- **Gate E (Final report & walkthrough)**: after finishing, transparently brief what changed and how it was verified.

**Domain logic rules** (same source, also summarized in the Architecture section above):
- Hard constraints (e.g. wheelchair access) are never relaxed. There is no B2C-era soft-constraint relaxation any more (time/distance/difficulty fallback tiers — intentionally removed). Don't reintroduce that without discussing it first — it was a deliberate cleanup, not an oversight. **As of 2026-07-25, `target_course`/`preferred_location`/`key_item_or_crop` are no longer soft-relaxed either** — a zero-match on any of those three is now a fail-fast rejection (`is_exit_early`/`exit_reason`), not a "drop the condition and keep searching unfiltered" fallback. `fallback_applied`/`fallback_reason` in `AgentState` are now effectively dead on this path (always `False`/`None`) — this reverses what used to be documented here as a "still-live" mechanism; don't reintroduce the relaxation behavior without discussing it first, same as the hard-constraint rule above. See the `retriever` bullet in the Architecture section for the full mechanics.
- `market_location_resolver` must only resolve a region to one that Olle courses actually pass through (`_get_olle_relevant_admin_dongs`, in `src/services/db_service.py` since 2026-07-26 — was in `nodes.py` before that) — never let it pick a statistically-top region with no course coverage (e.g. downtown Jeju-si dongs like 연동/노형동) just because it ranks highest on some `visitor_analytics` metric.

**File link rules** (when referencing a file in a reply):
- Only link files that actually exist at the given path, with the correct extension — never a made-up or unverified path.
- Use a real `file:///c:/Users/.../path.py`-style URI (forward slashes) or, inside a markdown doc, a path relative to that doc (`./` or `../../`).
- URL-encode spaces (`%20`) or wrap the path in `< >` if it contains spaces/special characters.
- Leave a space after a markdown link's closing `)` before any Korean particle (은/는/이/가/을/를/의/에 etc.) so the link doesn't get mis-parsed.

**Other**
- `docs/` is gitignored — files there won't show up in `git status`; don't assume doc edits are tracked/committed.

## Agent Team Harness: jeju-olle-dev-team

**목표:** 이 프로젝트의 코드 수정(버그 수정/기능 추가) → 라이브 서버 검증 → 문서 동기화를
3인 에이전트 팀(`jeju-node-developer`/`jeju-live-qa`/`jeju-docs-sync`)으로 조율해 경계면
버그(라우팅↔노드, 파싱↔소비)를 유닛 테스트 통과 이후에도 놓치지 않는다.

**트리거:** 실질적인 코드 변경/검증/문서화 작업 요청 시 `jeju-olle-harness` 스킬을 사용하라
(버그 수정, 기능 추가, QA 시나리오 실행, 문서 최신화 등). 코드를 이해하기 위한 단순 질문은
이 스킬 없이 직접 응답 가능.

**변경 이력:**
| 날짜 | 변경 내용 | 대상 | 사유 |
|------|----------|------|------|
| 2026-07-25 | 초기 구성 (jeju-node-developer/jeju-live-qa/jeju-docs-sync 3인 팀) | 전체 | 세션 내내 반복된 "구현→라이브 검증→발견된 새 버그 재수정→문서화" 사이클을 체계화 |
| 2026-07-25 | retriever 무조건 반려(Fail-Fast) 정책 도입 + 동명 법정리(세화리 등) 충돌 근본 수정(`eup_myeon_dong_areas` 신규 컬럼+백필) + `_fetch_market_insight` 접미사 정규화. 3라운드에 걸쳐 팀이 구현→라이브 QA→후속 버그 발견→재수정→재검증을 반복. 라이브 QA로 새로 발견된 별개 버그 2건(작물 교차 오염, quality_checker 컨텍스트 누락)은 사용자 결정으로 이번 세션에서는 보류 | retriever/quick_responder/tool_agent/check_quality_node, courses 스키마 | 사용자가 fail-soft 완화 대신 무조건 반려로 정책 전환을 명시적으로 결정; 그 검증 중 드러난 지역 매칭 버그를 근본 수정하기로 사용자가 선택 |
| 2026-07-26 | 07-25 회고록이 사용자 판단으로 보류했던 버그 2건(`generate_report_node` 작물 교차 오염, `check_quality_node` 기획서 경로 검증 컨텍스트 누락) 수정 + 실 서버 라이브 검증(4개 다중 코스 매칭 질의로 작물 귀속 정확성 확인, Market Insight 수치 정확 인용 + 3/4 1회 통과 확인, 회귀 스팟체크 2건 정상). 재시도 비결정성은 완전히 제거되지 않음(quality_checker가 LLM 기반이라 실행마다 통과 여부가 갈림)을 문서에 정직하게 반영 | `generate_report_node`/`check_quality_node` (`src/agent/nodes.py`) | 사용자가 회고록의 후속 작업으로 이 두 deferred 버그의 수정 우선순위를 선택 |
| 2026-07-26 | 위 항목의 후속: 사용자가 섹션 3(로컬 상생 제휴)의 비짓제주 API 조회 범위를 검색된 전체 코스에서 1순위 매칭 코스(`chunks[0]`)로 직접 제한(`unique_combos` 수집 루프 수정, 회귀 테스트 3건→2건/1건으로 기댓값 조정). 그 직후 발견된 잔여 불일치(섹션 1의 단가 산정용 `_price_combo_set`이 여전히 전체 `chunks`를 세고 있어 섹션 3에 실제로 없는 조합까지 가격에 반영될 수 있었음)를 같은 범위로 수정하고 회귀 테스트 추가, CLAUDE.md의 "section 3는 전체 코스를 섞는 게 의도된 설계"라던 이전 기록을 정정 | `generate_report_node` (`src/agent/nodes.py`) | 사용자가 직접 적용한 범위 축소 변경사항을 검증하는 과정에서 발견된 파생 불일치를 같은 원칙으로 정합화 |
| 2026-07-26 | `strict_single_crop`(독점 작물 한정) 옵션 신규 구현: "당근만"/"오직 마늘만" 같은 배타적 단일 작물 표현일 때만 `_resolve_effective_crops` 가드로 1순위 코스의 공동 재배 작물을 완전히 제외(fail-soft — 지정 작물이 실제 코스에 없으면 미적용), 배타 표현 없는 단순 작물 지정은 기존처럼 전체 crops 노출 유지(사용자 확정 제약). 라이브 QA 도중 이 기능과 무관한 별개 버그(intent_classifier가 "추천" 없이 "코스"+"기획서"만 있는 질의를 3회 중 2회 `other`로 오분류)를 발견, 사용자 승인 받아 같은 세션에서 `_rule_based_precheck`에 결정론적 규칙 추가로 수정. 개발→라이브 QA→(버그 발견)→재개발→재검증 2라운드 후 전부 통과, 신규 버그 없음. 최종 pytest 265건/ruff 신규 에러 카테고리 없음 확인 | `src/models/schema.py`, `src/agent/nodes.py`(`parse_intent_node`/`generate_report_node`), `src/agent/router.py`(`_rule_based_precheck`) | 사용자가 계획서로 제시한 기능 요구사항 구현; QA 중 발견된 별개 라우팅 버그는 사용자가 "지금 바로 수정"을 선택 |
| 2026-07-26 | 사전 의도 분류(Stage 0) 카테고리를 5개(`course_info`/`course_recommendation`/`olle_general_info`/`other`/`info_lookup`)에서 3개(`course_recommendation`/`info_lookup`/`other`)로 통합. 리더가 코드로 직접 검증한 근거: `graph.py`/`nodes.py`/`main.py`의 모든 라우팅·노드 로직이 애초부터 `==course_recommendation` 또는 `==other`(혹은 `!=`)만 검사했고, 나머지 세 값을 개별 분기하는 코드는 어디에도 없어 다운스트림은 완전히 무변경 — 순수 라우터 계층 정리. `_rule_based_precheck` 규칙 (c)(특정 코스 의견 문의)의 반환값도 `course_info`→`info_lookup`으로 변경. 라이브 QA 6개 시나리오(코스 메타 조회/일반 정보/문화 지식/특정 코스 의견 문의 핵심 회귀/strict_single_crop 스모크/코스 추천 거절 회귀) 전부 통과, 신규 버그 없음. pytest 265건(통합 전후 건수 동일 — 삭제 없이 리네이밍/값 교체), ruff 신규 에러 카테고리 없음 | `src/models/schema.py`(`IntentCategory`), `src/agent/router.py`(`_rule_based_precheck`/`SYSTEM_PROMPT`), `tests/test_router.py`/`test_graph_routing.py`/`test_tool_agent.py` | 코드 검증 결과 다운스트림이 이미 3분기 기준으로만 동작하고 있었음이 확인되어, 사용자 결정으로 분류값 자체를 실제 분기 수만큼 단순화 |
| 2026-07-26 | **순수 리팩토링(동작 변경 0건)**: `nodes.py`(2447→1965줄)에 누적된 DB 조회/보정 헬퍼를 신규 서비스 레이어 `src/services/db_service.py`(538줄)로 verbatim 이관. 사용자 지정 11개 심볼(`_looks_like_course_name`/`_execute_rdb_filtering`/`_fetch_market_insight`/`_fetch_course_meta_by_name`/`_search_culture_knowledge`/`_resolve_stats_region_from_areas`/`_get_olle_relevant_admin_dongs`/`_get_latest_available_year_month`/`_query_visitor_analytics_row`/`_resolve_canonical_region_dong`/`_load_legal_to_admin_mapping_from_csv`) + 순환 import 방지·데이터 응집 목적의 의존 헬퍼·상수(문화지식 JSON 폴백 체인 전체 포함)를 함께 이동. 표현/검색-정책 레이어(`_MARKET_METRIC_LABELS`/`_build_course_meta_context_str`/`_filter_course_ids_by_*` 등)와 `get_supabase_client()` 호출부는 노드에 그대로 유지 — 모든 헬퍼가 이미 `client`를 인자로 받는 구조라 호출 시그니처 무변경. 이관 중 테스트 7곳이 깨졌는데 원인은 **db_service 내부 함수끼리의 호출은 nodes에 패치해도 가로채지지 않는다**는 점(`quick_responder_node`→`_resolve_stats_region_from_areas`→`_get_olle_relevant_admin_dongs` 간접 경로)이었고, 패치 대상 모듈을 `src.services.db_service`로 옮겨 수정. pytest 268건(기준선 265+신규 3) 전부 통과, ruff 1383건이 카테고리별 개수까지 기준선과 완전 일치(E501 1347/I001 20/F401 11/E402 2/F541 2/F841 1 — 신규 findings 0건), 라이브 스모크 2건 통과("한림 기획서 만들어줘" retriever→report_generator 경로 / "7코스 초보자한테 추천할 만해?" quick_responder→tool_agent 경로, 노드 라우팅·로직 동작 완전 동일) | 신규 `src/services/db_service.py`·`src/services/__init__.py`·`tests/test_db_service_layer.py`, 수정 `src/agent/nodes.py`·`src/agent/tools.py`·`scripts/backfill_eup_myeon_dong_areas.py` + 테스트 9종 | 노드 파일 한 곳에 DB 접근·표현·정책 로직이 모두 누적돼 있어 데이터 접근 계층을 분리 |
| 2026-07-26 | **순수 리팩토링 2탄(동작 변경 0건)**: 위 `db_service` 분리의 후속으로 **단가 산정 로직**을 같은 `src/services/` 아래 신규 `price_estimation_service.py`(144줄)로 이관, `nodes.py` 1965→**1860줄**. 상수 7개(`_PRICE_BASE_FLAT`/`_PRICE_BASE_RATE_PER_HOUR`/`_PRICE_DIFFICULTY_MULTIPLIER`/`_PRICE_ADDON_PER_COMBO`/`_PRICE_ADDON_MAX_COMBOS`/`_PRICE_GROUP_SIZE_BY_AUDIENCE`/`_PRICE_AUDIENCE_LABEL`) + 함수 4개(`_compute_price_breakdown`/`_estimate_price_range`/`_build_price_breakdown_str`/`_resolve_effective_crops`)를 이동하고 `git show HEAD:src/agent/nodes.py`와 **바이트 단위 동일**함을 확인 — 산술 공식 무변경. `_resolve_effective_crops`는 이름상 가격과 무관해 보이지만 단가의 입력인 조합 수를 좌우하는 같은 계산의 앞단이므로 같은 모듈에 배치(호출부는 `generate_report_node` 단 1곳, `_effective_top_course_crops`로 캐싱 후 단가 조합 수/섹션 3 조회·프롬프트/`strict_single_crop` 절대규칙 3곳에서 재사용하는 구조 유지). `nodes.py`는 3개 함수만 import(`_compute_price_breakdown`·`_PRICE_*` 상수는 노드가 직접 참조하지 않음), 호출부는 한 줄도 수정 안 함. **직전 세션과 달리 "패치 대상 이동" 함정은 발생하지 않았음** — 이 4함수/7상수를 목킹하던 테스트가 원래 없었고(전부 직접 호출 검증), 신규 `tests/test_price_estimation_service.py` 22건도 목킹 없이 순수 함수를 직접 호출. 검증: `pytest` **290건 통과**(기준선 268+신규 22, 실패 0), `ruff check .` 1383건 카테고리별 개수까지 기준선과 완전 일치(신규 findings 0건), 라이브 스모크 2건 통과("당근만을 활용한 코스 기획서 써줘" → 21코스 `strict_single_crop` 정상·감자 언급 0회·섹션 3 체험 3건과 단가 콤보 수 일치 / "한림 기획서 만들어줘" → 15-B코스 단가·내역 정상·Market Insight 한림읍 559,007명 정확 인용·14코스 작물 미오염) | 신규 `src/services/price_estimation_service.py`·`tests/test_price_estimation_service.py`, 수정 `src/agent/nodes.py`(import 3줄)·`tests/test_generate_report_node.py`(import 경로 1줄) | `generate_report_node` 본문에 단가 산술과 리포트 조립 로직이 섞여 있어 순수 계산 계층을 분리(테스트도 목킹 없이 직접 검증 가능해짐) |
| 2026-07-26 | **순수 리팩토링 3탄(동작 변경 0건)**: 같은 날 세 번째 구조 정리로 단일 파일 `src/agent/nodes.py`(1860줄)를 **`src/agent/nodes/` 패키지 6개 서브모듈로 분할**(`intent.py` 201 / `safety.py` 67 / `retriever.py` 485 / `reporter.py` 644 / `quality.py` 235 / `tools.py` 244 / `__init__.py` 157) — `nodes.py` 파일 자체는 삭제. `__init__.py`가 노드 함수 11개 + 옛 모듈 전역 헬퍼까지 총 53개 이름(노드 11 + 헬퍼·클라이언트 42)을 `__all__`로 재노출해 `from src.agent.nodes import X` 임포트 경로와 `patch.object(nodes, X)` 목킹 대상을 **한 글자도 바꾸지 않음**. 핵심 설계 결정은 **방어적 로컬 임포트** — 서브모듈이 공유 이름을 쓸 때 모듈 최상단이 아니라 **함수 본문 안에서** `from src.agent.nodes import ...`로 가져와(12곳), 호출 시점에 패키지 속성으로 이름이 해석되게 함(최상단 임포트였다면 import 시점 바인딩으로 기존 목이 전부 무력화됐을 것). 그 결과 **테스트 파일 수정 0건으로 290건 전부 통과** — 같은 날 `db_service` 추출 때 7건이 깨졌던 것과 대조적. 그린 테스트만 믿지 않고 목 주입 스크립트로 12/12 로컬 임포트 호출부가 실제로 가로채짐을 직접 확인. 동시에 하드코딩 대형 시스템 프롬프트 7종을 `src/agent/prompts/*.md` + `loader.py(load_prompt)`로 자원화(`router.py`의 하드코딩 `SYSTEM_PROMPT` 삭제 → `load_prompt("router.md")`; 단 **부분 이관** — `nodes/quality.py`의 검증/재작성 프롬프트 3종은 인라인 리터럴로 유지), 6종은 HEAD와 바이트 단위 일치·`generate_report.md`만 f-string→`.format()`(13개 플레이스홀더) 전환에 따른 의도된 2줄 차이. 검증: pytest **290/290**(테스트 수정 0건), `ruff check .` **1312건으로 오히려 감소**(기준선 1383 — E501 1347→1277, I001 20→19, 신규 카테고리 0), 라이브 5개 시나리오 전부 통과(기획서 생성/quick_responder/strict_single_crop/거절 회귀/fail-fast 회귀 — DB 실값 정확 반영, 작물 교차오염 없음, `quality.py`↔`reporter.py` 서브모듈 왕복 경로가 실제로 해석되는지 Self-RAG 별점 치환으로 확인) | 신규 `src/agent/nodes/`(7파일)·`src/agent/prompts/`(md 7종+`loader.py`+`__init__.py`), 삭제 `src/agent/nodes.py`, 수정 `src/agent/router.py` | 1860줄 단일 노드 파일의 역할별 분리 + 프롬프트를 코드에서 분리해 문구 수정 시 파이썬을 건드리지 않도록 자원화 |
| 2026-07-27 | **`AgentState` TypedDict 세분화(순수 타입 강화, 동작 변경 0건) + 그 과정에서 라이브 QA가 발견한 NULL 크래시 경로 수정.** ① `src/agent/state.py` **단일 파일만** 수정해 `Dict[str, Any]`/`List[Dict[str, Any]]`로만 선언돼 있던 필드들을 중첩 TypedDict 16종(`HardConstraints`/`ParsedConstraints`/`MarketLocationQueryDict`/`MarketLocationResolutionDict`/`B2BQueryParamsDict`/`CourseMetaDict`/`WeatherInfoDict`/`SafetyCheckDict`/`RetrievedChunkDict`/`CultureChunkDict`/`SubSegmentDict`/`MarketInsightDict`/`RecommendationDict`/`QualityReportDict`/`ToolCallDict`/`ToolOutputDict`)으로 명시하고, 각 독스트링에 "누가 채우고 누가 읽는가"를 파일 단위로 기록. **핵심 설계 결정 — `typing.NotRequired`(3.11+)를 쓸 수 없는 환경**(`target-version = "py310"`)이고 `typing_extensions`는 pydantic 전이 의존성으로 설치돼 있을 뿐 `requirements.txt`에 없어, 새 의존성 대신 **stdlib만으로 되는 `total=True` 베이스(`_XxxRequired`) + `total=False` 서브클래스** 관례를 채택(3종에 적용: `B2BQueryParamsDict`/`WeatherInfoDict`/`MarketInsightDict`). 라이브로 `__required_keys__`/`__optional_keys__` 계산과 `get_type_hints(AgentState)` 24개 필드 = LangGraph 채널 24개 1:1 일치를 실제 확인. **노드 코드·테스트 파일 전부 무수정**(TypedDict은 런타임에 plain dict이라 `.get()`/`[...]`/부분 dict 반환이 그대로 동작함을 실증) — 이 시점 pytest 290/290 유지. 필수/선택 판정은 DDL이 아니라 **소비부의 접근 방식**을 기준으로 했고, 그 대조 과정에서 선언 정확성 4건을 코드 실제 동작에 맞춰 바로잡음: `QualityReportDict`의 피드백 키는 `reason`/`suggestions`가 아니라 **`feedback`**(3키 전부 선택 — 정상 경로가 `json.loads(llm_output)` 그대로라 `should_continue`가 `passed` 부재 시 fail-closed하는 이유), `WeatherInfoDict`에 계획에 없던 고정 상수 3필드(`temperature`/`precipitation_mm`/`wind_speed_ms`, 현재 소비처 없음) 추가 + `guideline`만 선택, `market_location_resolution`은 `B2BQueryParams` Pydantic 모델에 애초에 없고 `resolve_market_location_node`가 런타임 주입하는 값이라 유일한 선택 필드, `ParsedConstraints`는 런타임 dict(LLM JSON 전체)보다 **의도적으로 좁게** 선언(실제로 읽히는 `hard_constraints`/`vector_query` 2개만 — 버그 아님). ② 그 라이브 검증 중 QA가 발견한 **실제 크래시 경로**를 사용자 승인 후 수정: `courses.crops`/`administrative_areas`가 SQL NULL이면 `.get(key, "")`가 기본값이 아닌 **None을 그대로 반환**(기본값은 키 부재일 때만 적용)해 `_resolve_effective_crops`의 `.split(",")`에서 `AttributeError`가 나고, 그 호출부(`nodes/reporter.py`, try 미적용)를 통해 `src/main.py`의 SSE 응답 전체가 `event: error`로 죽는 경로. 2곳을 기존 `float(x or 0.0)`과 동일한 `or` 패턴으로 교체(`nodes/retriever.py:211-212`, `price_estimation_service.py:139`)하고 회귀 테스트 2건 추가. 현재 DB엔 NULL 행 0건(빈 문자열만)이라 지금까지 발동한 적 없는 잠재 버그였음. 검증: **pytest 292/292**(290+신규 2), `ruff check .` **1340건 · 카테고리 구성 기준선과 동일**(E501/I001/F401/E402/F541/F841 — 신규 카테고리 0). 증가분은 전부 `state.py`의 한국어 독스트링 줄(state.py 단독 HEAD 8건(E501 7+I001 1)→32건(E501 32, I001 0); 실제 문자 수는 88자 이내인데 ruff가 동아시아 문자를 폭 2로 계산해 걸림) — **사용자 결정으로 압축 없이 이대로 수용**(기존 1300여 건과 동일한 house-style noise). 라이브 스모크 3회(타입 검증용 5시나리오 + NULL-safe 수정 후 2시나리오 재확인) 전부 통과, DB 실값 정확 인용·작물 교차오염 없음·`strict_single_crop` 정상 | 수정 `src/agent/state.py`(①), `src/agent/nodes/retriever.py`·`src/services/price_estimation_service.py`(②), 신규 회귀 테스트 2건(`tests/test_retrieve_rag_node.py`·`tests/test_price_estimation_service.py`) | ① 어떤 노드가 어떤 키를 채우고 무엇을 읽는지 코드 전체를 grep해야만 알 수 있던 문제를 타입 선언으로 해소(런타임 위험 0인 순수 강화). ② ①의 선언을 DDL과 대조하다 드러난 잠재 크래시를 사용자가 즉시 수정하기로 선택 |
